import { Placeholder } from "../Placeholder";

export default function PortfolioPage() {
  return (
    <Placeholder
      name="Portfolio"
      milestone="M3"
      summary="Holdings, P&L, and upcoming events — the only screen where full equity curves render. Position snapshots are already syncing; the view lands with M3."
    />
  );
}
