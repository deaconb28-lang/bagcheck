import { Placeholder } from "../Placeholder";

export default function ReportsPage() {
  return (
    <Placeholder
      name="Reports"
      milestone="M4"
      summary="Quarterly reports, the Wrapped archive, and correlation surfaces. Arrives once the score has enough history behind it."
    />
  );
}
