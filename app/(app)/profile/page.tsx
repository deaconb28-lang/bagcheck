import { Placeholder } from "../Placeholder";

export default function ProfilePage() {
  return (
    <Placeholder
      name="Profile"
      milestone="M4"
      summary="Your archetype, records, public handle, and settings — including the style baseline the score is measured against."
    />
  );
}
